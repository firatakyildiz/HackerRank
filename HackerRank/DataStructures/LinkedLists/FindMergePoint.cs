using System;
using System.Collections.Generic;
using System.Text;

namespace HackerRank.DataStructures.LinkedLists
{
    /// <summary>
    /// Note : C# was not available in HackerRank, so I wrote Java instead.
    /// Link : https://www.hackerrank.com/challenges/find-the-merge-point-of-two-joined-linked-lists
    /// Difficulty : Easy
    /// </summary>
    class FindMergePoint
    {
        //int FindMergeNode(Node headA, Node headB)
        //{
        //    ArrayList<Node> listA = new ArrayList<Node>();
        //    ArrayList<Node> listB = new ArrayList<Node>();
        //    while (true)
        //    {
        //        if (headA == headB)
        //            return headA.data;
        //        if (listA.contains(headB))
        //            return headB.data;
        //        if (listB.contains(headA))
        //            return headA.data;
        //        if (headA != null)
        //        {
        //            listA.add(headA);
        //            headA = headA.next;
        //        }
        //        if (headB != null)
        //        {
        //            listB.add(headB);
        //            headB = headB.next;
        //        }
        //    }
        //}
    }
}
